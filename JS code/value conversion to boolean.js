var name=""; // value is empty anser will be false of else block
var name2;   // value is undefined answer will be false of else block
var name3="a" // value is defined answer will be true of if block executed.

if(name2){  // if representes true value excution in its block body
    alert(name3);
}
else{ // else represents false value execution in its block body
    alert(name3);
}