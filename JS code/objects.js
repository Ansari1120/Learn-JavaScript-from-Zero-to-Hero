
var arr=[2,2,3,4,5,6,1];
console.log(typeof arr); // knowing type of variable we have object type.
var art=67.1;
console.log(typeof art); // number 

// more manipulations with objets

let car ={  
     model : "A8",
    year : 2019,
    color : "Black",
    in_Stock : true
};

var updatehere=car.color;
console.log(updatehere);
 

// iterables 

for (const ton of arr) {
    console.log(ton);
}